services:
  auth:
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - Sentry:Dsn=
      - Trakt:ClientId=
      - Trakt:ClientSecret=
      - Spotify:ClientId=
      - Spotify:ClientSecret=
